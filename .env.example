# NPClassworks 默认使用当前站点的同源后端。分离部署时填写后端 HTTPS 根地址。
VITE_DEFAULT_KV_SERVER=
VITE_DEFAULT_SERVER_PROVIDER=kv-server

# C1 兼容功能默认关闭；仅在真实迁移旧 UUID/KV 数据时临时开启。
VITE_ENABLE_LEGACY_CLASSWORKS=false
VITE_DEFAULT_AUTH_SERVER=

# 学生/教师端行为分析与设备指纹，默认不加载。
VITE_ENABLE_ANALYTICS=false
